{
  "name": "the-visitors",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ rtomas/the-visitors",
    "create-local": "graph create --node http://localhost:8020/ rtomas/the-visitors",
    "remove-local": "graph remove --node http://localhost:8020/ rtomas/the-visitors",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rtomas/the-visitors"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0",
    "@protofire/subgraph-toolkit": "^0.1.2"
  }
}
